info:
  name: POST
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000
  body:
    type: file
    data:
      - filePath: ../bruno.png
        contentType: image/png
        selected: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
